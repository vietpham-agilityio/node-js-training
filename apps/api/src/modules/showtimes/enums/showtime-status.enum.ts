// BR-06: showtimes.status is 'scheduled', 'active', 'completed' or 'cancelled'
export enum ShowtimeStatus {
  SCHEDULED = 'scheduled',
  ACTIVE = 'active',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
